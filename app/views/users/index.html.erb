<h2>All users</h2>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| # można skrócić ten zapis do krótszego  render @users %>
  <%= render user %>
  <% end %>
</ul>

<%=  will_paginate %>

